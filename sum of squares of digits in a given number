#include <stdio.h>

int main()
{
    int n,rem;int sum=0;
    printf("enter the value of n");
    scanf("%d",&n);
    while(n!=0){
    rem=n%10;
    sum=sum+(rem*rem);
    n=n/10;
    }
    printf("%d",sum);


    return 0;
}
