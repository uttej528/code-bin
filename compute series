
#include <stdio.h>
#include<math.h>
int main()
{
    int i,x,n,sum=1;
    printf("enter the value of n");
    scanf("%d",&n);
    printf("enter the value of x");
    scanf("%d",&x);
    for(i=1;i<=n;i++)
    {
      sum=sum+pow(x,i);
        
    }
printf("%d",sum);
    return 0;
}
