//diff btw printf,scanf and puts,gets
#include <stdio.h>
int main()
{ char name[50];
 printf("enter your name");
 //scanf("%s",name);
 //printf("%s",name);
 gets(name);
 printf("your name is");
 puts(name);

    return 0;
}
