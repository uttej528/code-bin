#include<stdio.h> 
#include <math.h>  //area of rhombus
int main(){ 
   int p,q,area;
   printf("enter the values of p,q");
   scanf("%d%d",&p,&q);
   area=0.5*p*q;
   printf("area of rhombus is %d",area);
     return 0;
    }
